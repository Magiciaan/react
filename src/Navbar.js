const Navbar = () => {

    return (
        <nav className="Blog">
            <div className="links">
                <a href="/">Home</a>
                <a href="/create">Create</a>
            </div>
        </nav>
    );
}

export default Navbar;